import { Items } from './itemsPage/items';

export const TableteCategory = () => {

    const brands = [
        'Apple'
    ];

    const category = 'tablete';

    return <Items brands={brands} category={category} />
};
